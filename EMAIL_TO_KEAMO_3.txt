Subject: Ready to sign Payout — need credentials, docs & the agreement

Hi Keamo

Voucher issuing is fully built into our WhatsApp wallet and is now in live
customer testing — thank you again for the float and the quick turnarounds.

We're ready to move on two things. Payouts is our priority.

PAYOUT (our priority — we want to go live)

1. We're ready to sign the Payout agreement. Please send the version ready
   for signature and we'll turn it around quickly.

2. Payout API credentials + documentation — we currently hold only the
   documentation-portal password. Please send the actual API credentials and
   base URL so our engineers can start integrating in the sandbox now, in
   parallel with signing.

3. Payout activation checklist — once the agreement is signed, what remains
   to switch production on? (Float/prefunding amount, IP allowlisting,
   compliance/FICA checks, and expected timelines — whatever your standard
   onboarding path is.) We'd like to plan the go-live date.

REDEMPTION (merchant credentials confirmed working — just need the spec)

4. Merchant / Redemption API base URL + documentation. We've verified our
   WAPAYVMT merchant API key in the portal (it validates green), but there's
   no API documentation or base URL anywhere on the dashboard, and the
   merchant endpoints we tried aren't where we expected them (test-api
   returns "not found"; merchant-api refuses our requests with a 403, which
   looks like IP allowlisting). Please send:
     a) the correct Merchant/Redemption API base URL (test + production),
     b) the API documentation, and
     c) if there's an IP allowlist, how we register our egress IPs.
   The test vouchers you sent are ready and waiting — we can integrate
   redemption within days of receiving this.

5. Voucher status via API — the portal's "Lookup a Voucher" shows redemption
   status by serial number. Is that same status available through the API on
   vouchers we've issued? We want to show our customers which of their
   vouchers are still unspent, which drives repeat purchases.

LOOSE ENDS

6. The countersigned Merchant agreement copy for our records, and an ETA on
   the Hollywoodbets redemption issue you mentioned was being looked at.

7. The current list of platforms and merchants that accept OTT vouchers —
   it's what we show recipients so they know where to spend.

Thank you
Niev
